# ShadowCursor local environment example
#
# This repository does not require a .env file for standard extension development.
# User API keys are entered through the extension settings UI and stored in Chrome
# extension storage, not loaded from process environment variables.
#
# If you add local tooling around this repo later, keep real secrets out of source
# control and document them separately.
